#!/usr/bin/python3
def multiple_returns(setence):
    if sentence is not None:
        if len(sentence) == 0:
            return (0, None)
        else:
            return (len(sentence), sentence[0])
